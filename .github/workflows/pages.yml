name: Benchmark Report Pages

on:
  push:
    branches: [main]
    paths:
      - 'results/*.json'
      - 'index.html'
      - 'inspector.html'
      - 'arco/**'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Assemble pages artifact
        run: |
          mkdir -p _site/results
          cp index.html _site/
          cp inspector.html _site/
          cp results/classification.json _site/results/
          cp results/reasoning.json _site/results/
          cp results/recommender.json _site/results/
          cp -r arco _site/arco

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - uses: actions/deploy-pages@v4
        id: deployment
